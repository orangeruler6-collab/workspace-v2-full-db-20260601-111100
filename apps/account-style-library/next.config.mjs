/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/style-workbench",
  outputFileTracingRoot: import.meta.dirname,
  reactStrictMode: true,
  devIndicators: false
};

export default nextConfig;
